<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/empty_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RuStore">
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.GamesActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.AppsActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.GosActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.StartOnboardingActivity"
            android:exported="true">
        </activity>
        <activity android:name=".activities.Onboarding2Activity"
            android:exported="true">

        </activity>
        <activity android:name=".activities.Onboarding3Activity"
            android:exported="true"/>
        <receiver android:name=".apk.DownloadCompletedReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>

        </receiver>
    </application>

</manifest>